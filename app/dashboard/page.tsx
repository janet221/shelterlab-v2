import { sprint7DemoService } from "@/lib/living-lab/demo-data";
export default function ObservationDashboardPage(){const sessions=[...sprint7DemoService.state.sessions.values()];const metrics=[["Observation missions",sprint7DemoService.state.missions.size],["Students",1],["Shelters",1],["Published evidence",sprint7DemoService.state.publications.filter((item)=>!item.unpublishedAt).length],["Revision paths",sessions.filter((item)=>item.revisionNumber>1).length],["Average quality",Math.round(sessions.filter((item)=>item.qualityScore).reduce((sum,item)=>sum+(item.qualityScore?.total??0),0)/sessions.filter((item)=>item.qualityScore).length)]];return <main className="mx-auto max-w-6xl p-5 lg:p-8"><header className="border-b pb-4"><p className="text-xs font-semibold text-cyan-700">SYNTHETIC_DEMO · Living Lab operations</p><h1 className="mt-1 text-2xl font-semibold">Observation Dashboard</h1></header><dl className="mt-6 grid grid-cols-2 border-y sm:grid-cols-3 lg:grid-cols-6">{metrics.map(([label,value])=><div className="p-3" key={label}><dt className="text-xs text-slate-500">{label}</dt><dd className="mt-1 text-2xl font-semibold">{value}</dd></div>)}</dl><p className="mt-4 text-xs text-slate-500">All metrics are deterministic synthetic demo records and do not claim real learning, welfare, or adoption impact.</p></main>}
