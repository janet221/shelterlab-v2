import{z}from"zod";import{NextResponse,type NextRequest}from"next/server";import{requireDemoUser}from"@/lib/auth/demo-session";import{sprint9ADemoService}from"@/lib/inquiry/demo-store";
export async function GET(request:NextRequest,{params}:{params:Promise<{id:string}>}){try{const id=z.string().parse((await params).id);const publicView=request.nextUrl.searchParams.get("view")!=="internal";if(!publicView)requireDemoUser(request.headers.get("x-test-code"),["student","teacher","shelter_staff","admin"]);return NextResponse.json({report:sprint9ADemoService.getReport(id,publicView),publicView});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Report unavailable"},{status:404});}}
