import{z}from"zod";import{NextResponse,type NextRequest}from"next/server";import{requireDemoUser}from"@/lib/auth/demo-session";import{asInquiryActor,runAuditedInquiryMutation,sprint9ADemoService}from"@/lib/inquiry/demo-store";
export async function POST(request:NextRequest,{params}:{params:Promise<{id:string}>}){try{const actor=asInquiryActor(requireDemoUser(request.headers.get("x-test-code"),["student"]));const id=z.string().parse((await params).id);return NextResponse.json(await runAuditedInquiryMutation(()=>sprint9ADemoService.createRevision(actor,id,new Date())),{status:201});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Revision failed"},{status:400});}}
