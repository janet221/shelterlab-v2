import Link from"next/link";import{sprint9ADemoService}from"@/lib/inquiry/demo-data";
export default function TeacherInquiryList(){return <main className="mx-auto max-w-6xl p-5 lg:p-8"><p className="text-xs font-semibold text-cyan-700">SYNTHETIC_DEMO &middot; Teacher</p><h1 className="mt-1 text-2xl font-semibold">Inquiry Review Queue</h1><div className="mt-6 divide-y border-y">{[...sprint9ADemoService.state.projects.values()].map(project=><article className="grid gap-3 py-4 text-sm md:grid-cols-[1fr_180px_180px]" key={project.id}><div><strong>{project.title}</strong><p className="text-slate-500">revision {project.revisionNumber} · {project.inquiryType}</p></div><span>{project.status}</span><Link className="border px-3 py-2 text-center" href={`/teacher/inquiries/${project.id}`}>Review inquiry</Link></article>)}</div></main>}
