import Link from"next/link";import{sprint9ADemoService}from"@/lib/inquiry/demo-data";
export default function StudentInquiryList(){const projects=[...sprint9ADemoService.state.projects.values()];return <main className="mx-auto max-w-6xl p-5 lg:p-8"><header className="flex flex-wrap items-end justify-between gap-3 border-b pb-4"><div><p className="text-xs font-semibold text-cyan-700">SYNTHETIC_DEMO &middot; Student</p><h1 className="mt-1 text-2xl font-semibold">One Health Inquiry Projects</h1></div><Link className="border border-cyan-700 bg-cyan-700 px-4 py-2 text-sm font-medium text-white" href="/student/inquiries/new">Create inquiry project</Link></header><div className="mt-6 divide-y border-y">{projects.map(project=><article className="grid gap-3 py-4 text-sm md:grid-cols-[1fr_180px_180px]" key={project.id}><div><h2 className="font-semibold">{project.title}</h2><p className="mt-1 text-slate-500">{project.oneHealthDimensions.join(" · ")}</p></div><span className="font-mono text-xs">{project.status.toUpperCase()}</span><Link className="border px-3 py-2 text-center" href={`/student/inquiries/${project.id}`}>Open workspace</Link></article>)}</div></main>}
