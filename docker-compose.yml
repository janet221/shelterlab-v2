services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: shelterlab
      POSTGRES_USER: shelterlab
      POSTGRES_PASSWORD: shelterlab
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U shelterlab -d shelterlab"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
